# These run on Linux hosts/runners — force LF so CRLF from Windows checkouts never
# produces "bad interpreter" or YAML parse errors.
*.sh    text eol=lf
*.yml   text eol=lf
*.yaml  text eol=lf
*.json  text eol=lf
*.md    text eol=lf
